[core]
editor = emacs
excludesfile = ~/.gitignore

[user]
	email = alexcu@canva.com
name = Alex Cummaudo

[push]
default = matching

[pull]
rebase = false
merge = true

[rebase]
updateRefs = true

[gpg]
format = ssh

[filter "lfs"]
clean = git-lfs clean -- %f
smudge = git-lfs smudge -- %f
process = git-lfs filter-process
required = true

[color]
ui = auto

# Work
[include]
path = ~/.gitconfig.work
[maintenance]
	repo = /Users/alexcu/work/canva
