[core]
	editor = emacs
	excludesfile = ~/.gitignore
[user]
	email = alex.cummaudo@rea-group.com
	name = Alex Cummaudo
[push]
	default = matching
[branch]
	autosetuprebase = always
[pull]
	rebase = true
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
